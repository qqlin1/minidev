[project]
name = "minidev"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "openai>=3.7.0",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
